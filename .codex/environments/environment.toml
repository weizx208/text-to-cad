# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "text-to-cad"

[setup]
script = '''
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"

scripts/dev/agent-env-setup.sh
'''

[cleanup]
script = '''
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"

scripts/dev/agent-env-cleanup.sh --current-only --apply
'''
